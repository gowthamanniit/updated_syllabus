Introduction
Environment setup
Simple our first program 
Escape sequence
datatypes (Strings, Characters, Floats, Integers)
variables
comment line , single # , multiple #=  =#
opertors(6 types 1.arithmetic(+ - * / \ ^ % +x -x),
2. relational 
3. logical (&& || !)
4. assignment ( = += -= *= /= %= /= \= %= ^=)
5.vectorized dot operators, 
6. bitwise operator (& | ~ << >>))
String inbuilt functions
common control statements
- Conditional constructs Statements (if end, if else end,if elseif else end,nested if)
- looping constructs (for , while) 
  note: global variable usage
- control statements jumping(break,continue)
user defined functions
recursive functions
writing commands
Mathematical operations
Arrays[]



===================
https://docs.julialang.org/en/v1/
www.cocalc.com
run: shift + enter


print("")
println("")
typeof("")---> String
println(""" hai i am gowthaman "1" is my favour "2" is my number """)
println('$') --->Char
println('welcome') --> error
- julia is a high level language like python with the performance of a fast language like c
- julia is a grat choice for scientific computing with
  *) Excellent performance
  *) N-dimensional arrays
  *) parallel and distributed computing
- And it is also nice environment for general programming with:
  *) An active ecosystem of packages and good tools for managing then
  *) a rich type system
  *) Iterators, asynchronous tasks, and coroutines.
- joining operator : *
www.julialang.org/downloads

println(typeof(4)) ------>Int64
println(typeof(-8)) ----->Int64
println(typeof(4.4))----->Float64
println(typeof(-4.5)) ---->Float64
println(typeof(0.0))----->Float64
println(typeof(7.))------>Float64

julia mode:
1) REPL (interactive mode)
 one line command
2) script fileb(Non interactive mode)
open vscode or atom or sublime text
save file : filename.jl extension

run: terminal/command prompt : julia filename.jl

sample code:
# this is comment
name="world"
println("Hello $name")

the $ symbol within a string performs string interpolation,
meaning the value of the name variable is inserted directly 
into the string.
